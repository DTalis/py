import operators

# Using addOperator
result_add = operators.addOperator(10, 5)
print(f"Addition: {result_add}")

# Using divideOperator
result_divide = operators.divideOperator(10, 5)
print(f"Division: {result_divide}")

from operators import addOperator, divideOperator

# Using addOperator
result_add = addOperator(10, 5)
print(f"Addition: {result_add}")

# Using divideOperator
result_divide = divideOperator(10, 5)
print(f"Division: {result_divide}")